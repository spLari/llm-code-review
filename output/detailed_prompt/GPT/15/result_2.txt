{
  "winner": "code_2",
  "reasons": [
    "Separation of concerns: code_2 extracts the core passivation work into doPassivate, reducing duplication and making the high-level flow (notification + passivation) easier to follow.",
    "Asynchronous-friendly and more correct for async notifier semantics: passivateAsync composes notifier futures when listeners are present and otherwise avoids returning or waiting on unnecessary futures, which is more flexible for integration and avoids blocking callers.",
    "Easier to test and extend: the doPassivate helper isolates error handling, store writes, and stats updates so those behaviors can be changed or unit-tested independently.",
    "Explicit listener check improves performance/readability: checking notifier.hasListener(CacheEntryPassivated.class) documents intent and avoids creating/propagating extra CompletionStages when listeners are absent.",
    "Although slightly longer, code_2's structure and clearer responsibilities improve maintainability — future changes (metrics, retries, different notifier behavior) can be localized to small methods instead of editing an inline block."
  ]
}