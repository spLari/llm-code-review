After evaluating both code snippets, I provide the following assessment:

```json
{
  "winner": "code_2",
  "reasons": [
    "code_2 is easier to understand due to the extraction of the delete logic into a separate method `checkDelete(payload)`",
    "code_2 has improved readability by reducing nesting and grouping related logic together",
    "code_2 is more maintainable and easier to extend because of its modular structure"
  ]
}
```

Here's a concise justification:

1. **Easier to understand**: code_2 is more readable due to the extraction of the delete logic into a separate method `checkDelete(payload)`. This separation of concerns makes it clearer what the `poll()` method is responsible for.
2. **Changes in naming, structure, or style**: code_2 has a more modular structure, with a clear and concise method name `checkDelete(payload)`. This improves readability and maintainability.
3. **Easier to maintain and extend**: code_2 is more maintainable because the delete logic is now isolated in a separate method. This makes it easier to modify or extend the delete behavior without affecting the rest of the `poll()` method.

Overall, while both code snippets have similar functionality, code_2 is more modular, readable, and maintainable, making it the better choice.